// initialize role object
export const initializing: unique symbol = Symbol("initializing role")

// supervise incident in child actor
export const supervising: unique symbol = Symbol("supervising incident")

// improvise scene for unknown message selector
export const improvising: unique symbol = Symbol("improvising scene")

// selector of obituary message to inform about death of an actor
export const obituary: unique symbol = Symbol("mourning actor death")

// when the poison pill is thrown on stage, the performing actor terminates itself
export const poisonPill: unique symbol = Symbol("poison pill")
